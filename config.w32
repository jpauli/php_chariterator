// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("charit", "for charit support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("charit", "enable charit support", "no");

if (PHP_CHARIT != "no") {
	EXTENSION("charit", "charit.c");
}

